{
  "name": "lustreview",
  "version": "1.0.0",
  "description": "A web project utilizing Neon Database and Stripe integration",
  "type": "module",
  "private": true,
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "nodemailer": "^6.9.15",
    "stripe": "^22.0.0"
  },
  "scripts": {
    "build": "npm --prefix admin/tailadmin-free-tailwind-dashboard-template-main install && npm --prefix admin/tailadmin-free-tailwind-dashboard-template-main run build && node scripts/sync-admin-build.cjs && node scripts/prepare-public.cjs",
    "vercel-build": "npm run build",
    "start": "node scripts/start-all.js"
  }
}
